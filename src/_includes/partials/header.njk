{# 
  ============================================
  ШАПКА САЙТУ - УКРАЇНСЬКА КЛАСИЧНА ГІМНАЗІЯ
  ============================================
  Мега-меню з випадаючими списками
  ============================================
#}

<div class="container header-container">
  <a href="/" class="logo">
    <img src="/assets/img/logo.png" alt="Лого" class="logo-img">
    <div class="logo-text">
      <span>Українська класична гімназія</span>
      <small>Лубенської міської ради</small>
    </div>
  </a>
  
  <div class="burger" id="burger-btn">
    <span></span><span></span><span></span>
  </div>

  <nav class="nav" id="main-nav">
    <div class="close-menu-btn" id="close-btn">&times;</div>

    <ul class="nav-list">
      {% for item in navigation %}
        {% if item.type == 'link' %}
          <li class="nav-item">
            <a href="{{ item.url }}" class="nav-link">{{ item.label }}</a>
          </li>
        {% elif item.type == 'mega_dropdown' %}
          <li class="nav-item dropdown">
            <span class="nav-link">{{ item.label }} <span class="arrow">▾</span></span>
            <div class="dropdown-menu mega-menu">
              {% for column in item.columns %}
              <div class="mega-column">
                <h4>{{ column.heading }}</h4>
                {% for link in column.links %}
                <a href="{{ link.url }}" {% if link.external %}target="_blank"{% endif %} {% if link.style %}style="{{ link.style }}"{% endif %}>{{ link.label }}</a>
                {% endfor %}
              </div>
              {% endfor %}
            </div>
          </li>
        {% elif item.type == 'simple_dropdown' %}
          <li class="nav-item dropdown">
            <span class="nav-link">{{ item.label }} <span class="arrow">▾</span></span>
            <div class="dropdown-menu simple-menu">
              {% for link in item.links %}
              <a href="{{ link.url }}" {% if link.external %}target="_blank"{% endif %} {% if link.style %}style="{{ link.style }}"{% endif %} {% if link.highlight %}class="highlight-link"{% endif %}>{{ link.label }}</a>
              {% endfor %}
            </div>
          </li>
        {% endif %}
      {% endfor %}
    </ul>
  </nav>
</div>
